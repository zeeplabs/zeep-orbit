# TEMPLATE ONLY — NÃO COMMITAR COM VALORES REAIS.
# Criar o Secret fora do GitOps:
#
#   kubectl create secret generic zeep-orbit-secret \
#     --from-literal=DATABASE_URL="postgres://..." \
#     --from-literal=DASHBOARD_BOOTSTRAP_SECRET="change-me"
#
# Alternativas GitOps-safe: SealedSecrets, SOPS/age, External Secrets Operator.
apiVersion: v1
kind: Secret
metadata:
  name: zeep-orbit-secret
  labels:
    app: zeep-orbit
type: Opaque
stringData:
  DATABASE_URL: "postgres://user:password@host:5432/dbname?sslmode=require"
  DASHBOARD_BOOTSTRAP_SECRET: "change-me"
