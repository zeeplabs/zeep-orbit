apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Secret NÃO está no GitOps — criar manualmente antes do apply:
#   kubectl create secret generic zeep-orbit-secret \
#     --from-literal=DATABASE_URL="postgres://..." \
#     --from-literal=DASHBOARD_BOOTSTRAP_SECRET="$(openssl rand -base64 32)"
# Ver secret.example.yaml para referência de campos.
resources:
  - deployment.yaml
  - service.yaml
