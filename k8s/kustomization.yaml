apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Secret NÃO está no GitOps — criar manualmente antes do apply:
#   kubectl create secret generic zeep-core-secret \
#     --from-literal=DATABASE_URL="postgres://..." \
#     --from-literal=BILLING_JWT_SECRET="$(openssl rand -base64 32)" \
#     --from-literal=INVENTORY_JWT_SECRET="$(openssl rand -base64 32)"
# Ver secret.example.yaml para referência de campos.
resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml
