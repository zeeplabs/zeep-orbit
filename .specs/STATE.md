# STATE

## Decisions

- **AD-001**: `mcp-read-only-tools` implemented via 2 sub-agent batches (T1-T7, T8-T15) + fresh Verifier, all on `develop`, no push. 15 atomic commits (`6df417d`..`230450b`) + 1 doc-closure commit (`bfa33d8`). PASS — spec-anchored check clean, 5/5 injected mutations killed. `.specs/features/mcp-read-only-tools/validation.md` has full evidence.
- **AD-002**: `mcp-safe-mutation-tools` implemented inline (single ~8-task batch, no sub-agents) + fresh Verifier, all on `develop`, no push. 8 atomic commits (`4b54100`..`8e52c1c`) + doc commits (design/tasks approval, 2 independent Verifier passes, spec closure). PASS — 11/11 ACs spec-anchored, 5/5 injected mutations killed. New Handler methods (`AddTableColumnForUser`, `AddTableIndexForUser`, `CreateWebhookForUser`, `SaveEventMappingForUser`) are MCP-tool-only — no new REST HTTP routes wired, a real divergence from the original spec's Assumptions table, corrected during closure. `.specs/features/mcp-safe-mutation-tools/validation.md` has full evidence. Both minor gaps the second Verifier pass flagged (FK-cycle detection via the new incremental column path; superadmin/`CanReadAnyApp` edge case for the 4 new tools) were closed with 5 additional tests, gate re-run clean.
- **AD-003**: Independent Opus review of both MCP features (`eaa07d0..HEAD`, 37 files) — 0 blocking/high findings, 3 medium + 7 low. 9 of 10 fixed in 5 commits (`31c1951`..`b01f2be`): server-side logging for all internal tool errors, 4 mis-mapped webhook validation errors, JTI/webhook-token description clarity, discarded-query fix in `GetWebhook` REST, `ListAppMembers` axis-routing fix, anti-smuggling test for `RenameFrom`, fragile-description test hardened, translated READMEs synced. 1 finding (decode-before-auth order in `CreateWebhook`/`SaveEventMapping` REST handlers) deliberately left as-is — matches the codebase's already-dominant convention (`CreateAppTable` does the same), reverting would reduce consistency for no security gain.
- **AD-005**: The AD-004 history debt reproduced *outside* a release context: a `tlc-spec-driven` batch worker for `ai-build-chat` (T1-T7) ran in an isolated git worktree that had branched from an old `develop` point (`b01f2be`, before 5 later commits including the ROADMAP update). Rebasing that worktree branch onto current `develop` replayed an already-ancestor "`release: bump to v1.5.0`" commit as a *new*, unrequested commit — because it wasn't recognized as content-equivalent to whatever reconciled it into `develop` originally — which would have silently re-landed the full v1.5.0 version bump (`CHANGELOG.md`, `Chart.yaml`, `package.json`, `changelog.json`) onto `develop` a second time via a plain `--ff-only` merge, with no relation to the feature being merged. Caught before any push by diffing the fast-forward's file list against the feature's actual per-commit diffs (`git diff <old-tip> <new-tip> -- <release files>` showed non-empty when the range should have been feature-only) — not by tooling. **Fix applied**: reset `develop` back to its true pre-merge tip and `git cherry-pick <release-commit>..<worktree-tip>` instead of rebase+ff-merge, landing only the 7 real feature commits; verified with an empty diff against release-tracked files. **Standing mitigation until AD-004's root-cause reconciliation happens**: any worktree/batch-worker branch used for `tlc-spec-driven` Execute must branch from — and be rebased/merged against — the *current* `develop` tip, never an older point; and before fast-forwarding *any* rebased feature branch back onto `develop`, diff the release-tracked files (`CHANGELOG.md`, `charts/zeep-orbit/Chart.yaml`, `internal/dashboard/ui/package.json`, `internal/dashboard/changelog.json`, `.github/release-notes-*.md`) between old and new tip and confirm it's empty before treating the fast-forward as safe. **Reproduced a second time, same feature, Batch 2 (T8-T12):** that worktree was created from `main` instead of `develop` (agent tooling defaulted to the wrong base — cause not fully diagnosed, flagged as a follow-up to check); the batch worker itself noticed the missing T1-T7 code and self-corrected with a `chore(worktree): merge develop` commit, which is exactly the trap — merging `main` (post-release, has the v1.5.0 bump) with `develop` (pre-release-bump on this branch) produces a merge commit carrying `main`'s version of the release-tracked files as new content relative to `develop`, and that merge commit is technically a valid ff-mergeable descendant of `develop` (it has `develop` as one parent), so the same "diff the release files" check is what caught it, not commit-graph position. Fixed the same way: reset `develop` to its pre-merge tip, cherry-pick only the 5 real feature commits (skipping the merge commit and everything upstream of it). **Added mitigation**: when dispatching an isolated-worktree batch worker for this skill, verify explicitly which branch/commit the worktree was created from before the worker starts (`git -C <worktree> merge-base --is-ancestor develop HEAD || git -C <worktree> log -1 --format=%H` against expected `develop` tip) rather than trusting the worker's own self-correction — a worker mid-task can only fix symptoms it can see (missing files), not the history contamination that fix silently introduces. **Reproduced a third time, different feature (`ai-edit-chat`, Batch 1, T1-T9):** worktree again created from `main` (`HEAD` = `a4a8a73`, the AD-004 release-merge commit) instead of `develop` — confirms this is the isolated-worktree tool's own default base, not a one-off fluke, still not root-caused on the tooling side. This time the worker was explicitly pre-instructed (in its dispatch prompt) to check `git log -1 --format=%H` against the expected `develop` tip *before making any commit* and to stop rather than self-correct if it mismatched — it did exactly that, reported back with zero commits made, zero risk of contamination. Confirmed safe to fix by `git reset --hard <expected-tip>` in that same worktree specifically *because* zero commits existed yet (moving an unused branch pointer, not a merge/rebase) — re-dispatched with that fix as an explicit first step. **Standing mitigation, superseding the previous one**: never trust an isolated-worktree worker's default base; every dispatch prompt for this skill must include the expected base commit hash and an explicit instruction to verify-before-first-commit and stop-don't-self-correct on mismatch — this is now the required dispatch pattern, not optional advice.
- **AD-006**: `ai-edit-chat` shipped (edit an existing app via chat — add table/column/index/reference, toggle RLS per table, toggle app auth). Full Specify → Design → Tasks → Execute (2 batches, T1-T12) → Verifier (2 rounds: FAIL on AIEC-18 coverage gap + 1 surviving mutant → fix → PASS) cycle. Commits `2a11764..a4d2d6a`. `.specs/features/ai-edit-chat/validation.md` has full evidence. Known gap, deliberately unfixed: a non-owner `app_members` editor/admin with real `CanWrite()==true` doesn't see the "Edit with AI" button — no endpoint exposes "my role on this app" to a non-owner yet. Fails closed, usability gap only.
- **AD-007**: `column-foreign-key` shipped (add/remove FK on an existing table column — REST `*ForUser` handlers, MCP tools, "Edit with AI" chat wiring) plus a fix for a silent no-op in `PUT /tables/{id}` full-replace (accepted a `References` change on an existing column, "applied" without error, never ran the DDL — now rejected via `ReferenceConfig.Equal`). Executed via 2 sub-agent batches, 14 atomic commits (`1a0467d..ffbf9ba`) + a 3-iteration fix→re-verify Verifier loop (`0164688`, `4b3af05`, `f187ea2`) before PASS — 21/21 ACs, 4/4 injected mutations killed, zero production-code defects found (both gaps were test-coverage gaps: `DropColumnForeignKey`'s genuine-query-error path had no test; the first CFK-04 fix attempt was a false-positive test that passed via the wrong code path because it never set `AuthEmailEnabled: true`). `.specs/features/column-foreign-key/validation.md` has full evidence, diff range `76b37a0..f187ea2`. **Known history-cleanliness debt, not fixed**: commit `d2f0fdf` bundles `spec.md`'s creation with 5 unrelated pre-existing bug-fix files from a prior session (`ai_edit_chat_handlers.go`, `ai_edit_chat_handlers_test.go`, `apps_store.go`, `handler.go`, plus 6 frontend files) — a one-commit-per-task discipline violation. Already pushed to `origin/develop`; user chose not to rewrite history to split it. `.specs/STATE.md` itself lagged this feature by one session (D-217..D-223 tracked only in the Obsidian vault, not here) — reconciled by this edit.
- **AD-004**: Released **v1.5.0** (minor — new MCP tools warrant it over a patch). PR #14 `release-v1.5.0` → `main`: `gh pr merge --rebase` failed with a **real, reproducible** conflict (not a stale-ref artifact — reproduced 3× with fresh `git fetch`) on the very first commit unique to `develop` (`e91ac28`, `column-type-change-feedback`, 2026-07-29). Root cause: several old releases (`v0.5.0`..`v1.3.0`, commits `a8cfd40`/`948ca16`/`30ff8f2` etc.) were landed on `main` via **squash/merge-commit, not rebase**, so `main`'s tree for that content has different commit hashes than `develop`'s originals; `develop` later got a manual reconciliation merge (found as an unpushed local branch `backmerge-main-into-develop`, now deleted — its content was already folded into `origin/develop`, just never as a fast-forwardable rebase chain). A linear rebase replays pre-merge commits individually and re-hits the exact conflict the old merge already resolved once. Deliberately **used `gh pr merge --merge --admin` instead of rebase** for this one release, with user's explicit sign-off — GitHub's merge strategy landed as commit `a4a8a73` ("Merge pull request #14"), NOT a fast-forward, so `main` has one non-linear merge commit now (documented exception to the AGENTS.md §2 linear-history rule, not a silent violation). Tag `v1.5.0` pushed, `docker-publish.yml` green (image + GHCR + GitHub Release), `docs.yml` (Helm) green. **Open technical debt at the time, since resolved — see AD-008.**
- **AD-008**: Re-investigated the AD-004 history debt (user asked to "reconcile main→develop now", 2026-08-25 session). **Found it already resolved, no rebase/force-push needed.** `git merge-base main develop` returns `a8ed2cb` — the exact current tip of `main` — meaning `main` is a literal ancestor of `develop` with zero hash divergence anywhere in history. This happened as a side effect of D-225's `v1.6.0`-cut reconciliation merge (`develop` merged `--no-ff` back with `main` after the release, one trivial `CHANGELOG.md` conflict resolved by keeping `main`'s header). Practical effect: any future release-branch `gh pr merge --rebase` will only ever replay commits genuinely unique to `develop` since `main`'s current tip — it can no longer hit the old squash-merged-release conflict, because none of that conflicting history is behind an unreconciled boundary anymore. Confirmed via `git merge-base --is-ancestor main develop` (true) and by comparing `git merge-base main develop` byte-for-byte against `git rev-parse main`. No git operation performed — this was pure verification, zero risk. **AD-005 is unaffected and still active** — it documents a different, unrelated bug (isolated-worktree tooling defaulting to the wrong base branch), not the main/develop hash divergence AD-004 described. Credential rotation for the `asset-manager`/`starbem-apps` leak (D-187..190 in the vault) remains delegated to the user, marked done from the agent's side.

- **AD-009**: `column-enum-type` shipped — new `enum` column type (CHECK-constraint-backed, not native Postgres `ENUM`, for lock-avoidance reasons) declarable at column creation (table create or add-column), widened/narrowed via a dedicated endpoint (Dashboard action, REST `PATCH .../columns/{col}/enum-values`, MCP `orbit_update_column_enum_values`) — mirrors the AD-007 pattern (dedicated endpoint + Postgres-catalog lookup, never a full-table `PUT` or an assumed constraint name) rather than introducing a new one. Also usable from AI build/edit chat. Full Specify → Design → Tasks → Execute (3 sub-agent batches, T1-T16) → Verifier cycle. 17 commits (`93ddd1e`..`12b55c4`), all on `develop`, not pushed. Verifier: **PASS**, 16/16 ACs spec-anchored, 4/4 injected mutations killed. `.specs/features/column-enum-type/validation.md` has full evidence. **Design-doc-vs-reality gap, recurring across all 3 batches** (distilled as lessons L-036/L-037/L-038): `design.md` described (a) a constraint-lookup query shape that doesn't work against Postgres's actual catalog for CHECK constraints (`information_schema.key_column_usage` only covers PK/UNIQUE/FK — the real fix uses `pg_constraint`/`contype='c'`/`conkey`), (b) a "FK edit-action UI pattern" in `TableCard.tsx` that doesn't exist (FK edits go through the generic form like every other field), and (c) a `PlanColumn`/`References`-passthrough precedent for the AI-chat tool schema that also doesn't exist. None were blocking — each batch caught its own gap empirically and self-corrected with a documented `SPEC_DEVIATION`, and the Verifier confirmed each fix is real and correctly tested — but Design's own claims should be verified against the real code before an implementer trusts them, not just read as fact.

## Handoff

- **Feature**: `column-enum-type` — **DONE, verified PASS, committed to `develop`, NOT pushed**. New `enum` column type (CHECK-constraint-backed) declarable at creation, widened/narrowed via a dedicated endpoint (Dashboard/REST/MCP), usable from AI build/edit chat. Full Specify → Design → Tasks → Execute (3 sub-agent batches, T1-T16) → Verifier cycle this session. See AD-009 for the full decision record.
- **Phase / Task**: Execute complete, Verifier PASS on the first pass (no fix→re-verify round needed). Nothing active.
- **Completed this feature**: 16 task commits `93ddd1e`..`d4e3321` (T1-T16, one per task) + `12b55c4` (docs: spec/design/context/validation artifacts, written earlier but only committed at closing). `.specs/features/column-enum-type/{spec,context,design,tasks,validation}.md` all present. 16/16 ACs (CENUM-01..16) verified, 4/4 injected mutations killed, full repo gate green (`go build`, `go vet`, `gofmt -l`, `go test -p 1 -parallel 4 ./...`, frontend `tsc -b`+`build`, both locale JSONs valid).
- **Known residual, not fixed (non-blocking, per Verifier)**: 3 minor observations logged in `validation.md`, none warranting a fix task — (a) T13's deviation comments in `TableCard.tsx` aren't tagged with a literal `SPEC_DEVIATION` marker, so a repo-wide grep for that string misses them; (b) `ErrColumnIsNotEnum` reaches MCP clients with its `"dashboard: "` package prefix while the REST route strips it — cosmetic inconsistency, not a leak; (c) `TableCard.tsx`'s existing-enum UI (read-only chips + edit action) is gated on `table.id` being truthy — defensive-only, no known path where it's falsy for a real table.
- **Gate environment note (not this feature's bug)**: a bare `go test ./...` in a fresh shell fails ~36 webhook tests with `crypto: neither WEBHOOK_TOKEN_ENCRYPTION_KEY nor DASHBOARD_BOOTSTRAP_SECRET is set` — CI sets this env var (`.github/workflows/reusable-ci.yml:26`), local shells don't by default. Re-running with either var set passes everywhere; zero enum tests were among the false failures. Worth a `CONTRIBUTING.md`/README note if this keeps tripping up fresh sessions.
- **Next step / open items** (not blocking, no user request pending):
  1. Push `develop` (17 commits ahead of `origin/develop`: `93ddd1e`..`12b55c4`) — not done yet, blast-radius rule requires explicit go-ahead.
  2. Ship this feature in the next release alongside everything already sitting under `[Unreleased]` in `CHANGELOG.md` (`ai-build-chat`, `ai-edit-chat`, `column-foreign-key`, `mcp-advanced-policy-tool`, `app-update-schema-drift-fix`, now `column-enum-type`). `v1.6.0` already shipped (`4f834b2`) covering everything before `mcp-advanced-policy-tool`.
  3. Run `repair.sql` against production `internal-portal-rh` — still not done, needs a human to execute it block-by-block per the runbook. `internal-portal-rh` remains blocked from Login/Storage/API-tab saves until (a) the `app-update-schema-drift-fix` code ships/deploys AND (b) the runbook is actually run.
  4. 3 minor gaps from `mcp-advanced-policy-tool`'s validation, not fixed (all Minor/non-blocking) — see prior session notes if revisited.
  5. `ai-edit-chat` follow-up, not fixed (deliberately): a non-owner `app_members` editor/admin with real `CanWrite()==true` doesn't see the "Edit with AI" button.
  6. `column-foreign-key` history-cleanliness debt (see AD-007) — commit `d2f0fdf` mixes unrelated files. Already pushed; left as-is per user's choice.
  7. Lesson candidates recorded: `L-026`..`L-038` — review via `lessons.py` next time a feature touches model-call payload construction, confirm/session-status guards, catalog-lookup-based DDL functions, spec-precision on error messages, MCP tool AC field assertions, SQL runbook idempotency, audit-log AC coverage, or (new, L-036..038) trusting a `design.md` claim about a catalog-lookup mechanism, an existing UI pattern, or a schema-passthrough precedent without checking it against real code first.
  8. Credential rotation for the `asset-manager`/`starbem-apps` leak — delegated to the user, tracked as done from this side.
- **Blockers**: none from this agent's side. Production `internal-portal-rh` remains blocked for the user until item 3 above happens.
- **Uncommitted files**: none (working tree clean)
- **Branch**: `develop`, 17 commits ahead of `origin/develop` (not pushed)
