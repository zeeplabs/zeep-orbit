interface:
  display_name: "Zeep Orbit"
  short_description: "Repo-specific patterns and workflows for zeep-orbit"
  default_prompt: "Use the zeep-orbit repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true